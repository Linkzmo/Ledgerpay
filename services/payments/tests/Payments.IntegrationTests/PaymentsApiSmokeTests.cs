namespace Payments.IntegrationTests;

public sealed class PaymentsApiSmokeTests
{
    [Fact(Skip = "Requires running dependencies (PostgreSQL, Redis, RabbitMQ).")]
    public void Placeholder()
    {
    }
}
